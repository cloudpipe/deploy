---
- hosts:
  - api_nodes
  - job_nodes
  vars_files:
  - ['vars.local.yml', 'vars.yml']
  roles:
  - common
  - docker

- hosts:
  - api_nodes
  roles:
  - api

- hosts:
  - job_nodes
  roles:
  - jobs
