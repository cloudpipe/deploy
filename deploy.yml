---
- hosts:
  - api_nodes
  - job_nodes
  vars_files:
  - ['secrets.yml', 'secrets.vault.yml']
  - ['vars.local.yml', 'vars.yml', 'secrets.yml']
  roles:
  - common
  - docker

- hosts:
  - api_nodes
  roles:
  - api

- hosts:
  - job_nodes
  roles:
  - jobs
